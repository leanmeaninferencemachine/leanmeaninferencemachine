# Data Directory

This directory is for runtime data. When LMIM runs, it creates:
- `models/` - Downloaded GGUF models
- `logs/` - Application logs
- `config/` - User configuration

These are .gitignored by default. The directory structure is preserved for reference.
